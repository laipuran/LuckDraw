<Page x:Class="LuckDrawWindow.LuckDrawPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LuckDrawWindow"
      mc:Ignorable="d">

    <StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Label FontSize="18">请输入抽取人数：</Label>
                    <TextBox x:Name="NumberTextBox" Height="30" Width="75" FontSize="18">1</TextBox>
                    <Button x:Name="GetNumberButton" Content="抽取" Click="GetNumberButton_Click" FontSize="18" Margin="20,0,0,0"></Button>
                </StackPanel>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ResultTextBlock" FontSize="18" Margin="0,20,0,0"></TextBlock>
                </ScrollViewer>
            </StackPanel>
        </Grid>
    </StackPanel>
</Page>
