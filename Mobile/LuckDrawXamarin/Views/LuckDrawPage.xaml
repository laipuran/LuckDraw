<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LuckDrawXamarin.Views.LuckDrawPage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <ScrollView VerticalOptions="Start" Margin="10,20,10,0" HorizontalOptions="Center">
            <StackLayout>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"></ColumnDefinition>
                        <ColumnDefinition Width="1*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Editor x:Name="NumberEditor" Placeholder="输入一个数字："></Editor>
                    <Button Grid.Column="1" x:Name="GetButton" Text="抽取" Clicked="GetButton_Clicked"></Button>
                </Grid>
                <Label x:Name="ResultLabel" Text="被暗中选定的同学:" FontSize="Large" Margin="0,10,0,0"></Label>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>